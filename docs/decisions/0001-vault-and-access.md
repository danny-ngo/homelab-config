# Vault and access

Secrets remain in Ansible Vault; host-key checking stays strict. Linux uses tagged Tailscale enrollment with DNS acceptance and Tailscale SSH disabled, retaining OpenSSH/LAN recovery.
