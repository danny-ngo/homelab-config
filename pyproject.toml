[project]
name = "homelab-config"
version = "0.1.0"
description = "Development tooling for the homelab Ansible configuration"
requires-python = ">=3.14,<3.15"
dependencies = []

[dependency-groups]
dev = [
  "ansible-core==2.20.7",
  "ansible-lint==26.6.0",
  "yamllint==1.38.0",
]

[tool.uv]
package = false
