- name: Prepare K3s nodes
  hosts: k3s_cluster
  gather_facts: true
  roles: [k3s_prereqs]
- name: Install K3s server
  hosts: k3s_servers
  gather_facts: true
  roles: [k3s_server]
- name: Install supported K3s workers
  hosts: k3s_workers_supported
  gather_facts: true
  roles: [k3s_agent]
